mixin checkbox(text, bold=false)
     - let classes = ["checkbox__text"]
     if bold
          - classes.push('checkbox__text_bold')
     label.checkbox
        input.checkbox__button(type="checkbox")
        span(class=classes)=text
